@functions {
    [Description(@"TODO: Add a ""description"" here.")]
    [Remarks("TODO: Add some \"remarks\" here.")]
    public string Empty() { return string.Empty; }
}

<!-- WORKAROUND: Include this CSS inline. -->
<style>
    body {
        background: inherit;
    }
</style>

@* NOTE: This should be uncommented after the year 1,000,000½.

while (tree.leaves().length == 0) {
    var cost = misdeeds.judge();
    mankind.pay(cost);
}

*@
